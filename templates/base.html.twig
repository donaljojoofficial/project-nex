
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}IntruNex{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        {# Global styles: Black background + neon green accents #}
        <style>
            body {
                background-color: #000; 
                color: #0f0; /* Neon green text */
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                margin: 0;
                padding: 0;
            }
            a {
                color: #00ff9f;
                text-decoration: none;
            }
            a:hover {
                color: #0ff;
            }
            header, footer {
                padding: 1rem 2rem;
                background: #050505;
                border-bottom: 2px solid #0f0;
            }
            footer {
                border-top: 2px solid #0f0;
                border-bottom: none;
                text-align: center;
                font-size: 0.9rem;
                color: #0f0;
            }
            main {
                padding: 2rem;
                min-height: 80vh;
            }
        </style>

        {# Place to load Encore-built CSS/JS if you have them #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>
        <header>
            <h1>🛡 IntruNex</h1>
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

        <footer>
            &copy; {{ "now"|date("Y") }} IntruNex Security Systems
        </footer>
    </body>
</html>
